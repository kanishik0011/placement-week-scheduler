services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - PLACEMENT_DB=/app/data/placement_scheduler.sqlite3
    volumes:
      - backend-data:/app/data
  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_BASE=http://localhost:8000
    depends_on:
      - backend

volumes:
  backend-data:

